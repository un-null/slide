{
  "name": "slide",
  "scripts": {
    "dev": "marp -w -s",
    "build": "marp",
    "pdf": "marp --pdf",
    "pptx": "marp --pptx",
    "pptx:editable": "marp --pptx-editable"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.1.0"
  },
  "marp": {
    "allowLocalFiles": true,
    "themeSet": "./src/ts-challenge/style.css"
  }
}
